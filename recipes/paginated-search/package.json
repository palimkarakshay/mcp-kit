{
  "name": "@mcp-kit/recipe-search",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Recipe: cursor-based pagination done right. search_records returns ONE page plus an opaque next_cursor you pass back to fetch the next page; get_record fetches one item by id. Fixed in-memory dataset, no network.",
  "license": "MIT",
  "bin": {
    "mcp-recipe-search": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@mcp-kit/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.76"
  }
}
